/**
 * @verino/web-component CDN entry point
 * ─────────────────────────────────────────────────────────────────────────────
 * Bundled by build-cdn.js via esbuild → dist/verino-wc.min.js.
 *
 * The component package self-registers `<verino-input>` as a side effect, so
 * the CDN build only needs to execute the module once in the browser.
 */

import './index.js'
